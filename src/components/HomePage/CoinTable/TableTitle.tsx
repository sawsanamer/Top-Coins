import { Typography } from "@mui/material";

const TableTitle = () => {
  return (
    <Typography variant="h6" sx={{ px: 3, pt: 2 }}>
      Top Coins
    </Typography>
  );
};

export default TableTitle;
